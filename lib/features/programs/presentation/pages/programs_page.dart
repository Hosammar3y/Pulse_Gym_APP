import 'package:flutter/material.dart';
class ProgramsPage extends StatelessWidget { const ProgramsPage({super.key}); @override Widget build(BuildContext context) => Scaffold(appBar: AppBar(title: const Text('Programs')), body: const Center(child: Text('Workout Templates and Exercise Library are delivered in Phase 3.'))); }
